#include <tunables/global>

profile hassio-addons-drink-tracker flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  network,
  capability net_bind_service,

  /usr/bin/** rix,
  /usr/local/bin/** rix,
  /bin/** rix,
  /etc/** r,
  /opt/drink-tracker/** r,
  /data/** rwk,
  /var/** rw,
  /tmp/** rw,
}

